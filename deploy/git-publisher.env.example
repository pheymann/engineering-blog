# Install as /etc/engineering-blog/git-publisher.env with mode 0600. Do not put
# a token or private key in this file; point Git/SSH at root-owned credential
# files stored outside this repository instead.
GIT_CONFIG_GLOBAL=/etc/engineering-blog/gitconfig
GIT_SSH_COMMAND=ssh -F /etc/engineering-blog/ssh_config
